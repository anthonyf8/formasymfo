{% extends 'base.html.twig' %}

{% block body %}

<div class="container flex-grow-1 container-p-y">
    <div class="row">
        <div class="col-md-8 m-auto">
            <div class="card mb-4">
                <div class="card-body">
                    <div class="card-title">
                        <h1 class="fw-bold" style="color: limegreen;">{{event.name}}</h1>
                    </div>
                </div>
                <ul class="list-group list-group-flush">
                    <li class="list-group-item">
                        <div class="d-flex justify-content-between mt-4">
                            <div>
                                <p>From <span
                                            class="fw-semibold">{{event.startAt|date('Y-m-d')}}</span> to
                                    <span class="fw-semibold">{{event.endAt|date('Y-m-d')}}</span></p>
                                <p class="my-0">Project: {{event.project ? event.project.name : '-'}}</p>
                                <p class="my-0">Organizations: {{event.organization is not empty ? event.organization|map((o) => o.name)|join(', ') : '-'}}</p>
                                <p>Accessible
                                    : {{event.publish ? 'Yes' : 'No'}}</p>
                            </div>
                            <p class="fst-italic">{{event.volunteer|length}} volunteer yet.</p>
                        </div>
                    </li>
                    <li class="list-group-item">
                        <p class="text-muted">{{event.prerequisites}}</p>
                        <p class="">{{event.description}}</p>
                    </li>
                </ul>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-md-8 m-auto justify-content-start">
            <a href="{{path('app_list_events')}}">
                <button class="btn text-white" style="background-color: limegreen;">Back</button>
            </a>
        </div>
    </div>
</div>

{% endblock %}
